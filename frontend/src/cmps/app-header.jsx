import {ThemeToggle} from './theme-toggle'

export function AppHeader() {

    return(
        <header className="app-header">
            <ThemeToggle />
            app header
        </header>
    )
}